import React from 'react';
import './styles.css';

const Header = (props) => {
    return (
        <div>
            <h1>
                Weather app
            </h1>
        </div>
    )
}

export default Header